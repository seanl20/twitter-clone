<%= turbo_stream.replace "follow-unfollow-button" do %>
  <%= render partial: "users/follow_button", locals: { user: @user, current_user: } %>
<% end %>

<%= turbo_stream.replace "user-section" do %>
  <%= render partial: "profile/profile_section", locals: { user: @user } %>
<% end %>